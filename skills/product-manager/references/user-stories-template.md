# User stories — <feature>

Shape: **As a `<user type>`, I can `<capability>` so that `<benefit>`.**
Acceptance criteria in Given / When / Then.

## Story 1 — <short title>
As a <user>, I can <capability> so that <benefit>.

**Acceptance criteria**
- Given <context>, when <action>, then <expected result>.
- Given <edge case>, when <action>, then <handled behavior>.

**Notes:** dependencies, out-of-scope, open questions.

<!-- repeat for 3–7 stories; keep each independently shippable and testable -->
